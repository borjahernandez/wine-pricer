# Copy to .env and fill in what you need. Nothing here is required for curation or the classical baselines.

# LLM preprocessing (scripts/tasting.py) and the frontier/agent models. Groq's free tier is enough.
GROQ_API_KEY=
OPENAI_API_KEY=

# Pushing the curated dataset and the fine-tuned adapter to the Hub. Needs write scope.
HF_TOKEN=

# Streaming the QLoRA fine-tune to Weights & Biases (also add it as a Colab secret)
WANDB_API_KEY=
